// Exercise 5.28 from section 5.10.3

/* Exercise 5.29
 * a) FIX: (ptr-> ival) != 0
 * b) FIX: ival != (jval < kval) but very weird, might need to spread.
 * c) FIX: ptr !=  0 && ptr->ival++;
 * d) (ival++) && ival
 * e) vec[ival] >= vec[ival +1]; ++ival;
 */

#include <iostream>
#include <string>

using std::string;

int main()
{
   return 0;
}
