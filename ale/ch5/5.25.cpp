// Exercise 5.25-5.27 from section 5.10.2

/* Exercise 5.25, 5.26
 * a) (!ptr == ( ptr-> next)) 
 * b) ch = ((buf[bp++]) != '\n')
 */


#include <iostream>
#include <string>

using std::string;

int main()
{
   string sob("wordy");
   string place( (s + s[s.size() -1]) == ('s' ? "" : "s"));
   return 0;
}
