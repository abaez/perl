// Example from section blarg

#include <iostream>
#include <string>

using std::string;

int main()
{
    std::cout << "Just a test" << std::endl;
    int ival =0;
    ival = 3.523 +3;//Implicit type conversion. Happens without knowledge
    if (ival) //Converts to bool
    while (std::cin)// Converts to bool also :)
    enum master { m1=2, m2=3, m4, m5};	// Enumeration, remember these things are just grouped const :).
    const size_t size=1024;

    return 0;
}


