//Examples in exercise 2.3.3

#include <iostream>
#include "Sales_item.h"

int main()
{
    //Examples for defining objects
    double sixfig, another; // defining two variables of type double
    int minute, second, hour, day; // definining 4 variables of type int.
    std::string super;       //defining only 1 variable for type string of namespace std.

    //Examples for initialization
    int valu(1024);     //direct-initialization <---- more flexible in usage than the copy
    int vala = 1024;    //copy-initialization


    return 0;
}
