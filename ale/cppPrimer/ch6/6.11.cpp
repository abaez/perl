// Exercise 6.11 from section 6.7

/* Exercise 6.11
 * a) was trying to get both a buff and string, should have named them with && and std::cin >> for the other, also. Might be good to split them.
 * b) this is fine
 * c) while (ptr== 0)
 * d) bool status = false;
while (find(word))
{
    word = get_next_word();
    status = true;
}
if (!status)
    cout << "Did not find any words\n";

#include <iostream>
#include <string>
#include <vector>

using std::string;
using std::vector;
*/


int main()
{
    std::cout << "Just a test :)";
    return 0;
}


