// Example from section 5.3
#include <iostream>
#include <string>

using std::string;

int main()
{
    std::cout << "Just a test" << std::endl;
    ~ // bitwise NOT
    unsigned char bits = 1;
    bits << 3; // left shift and vice versa, shift the bits by the counted number
    & // bitwise AND
    ^ // bitwise XOR
    | // bitwise OR

    return 0;
}


