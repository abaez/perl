// Example from section 5.2

#include <iostream>
#include <string>

using std::string;

int main()
{
    std::cout << "Some examples from logical and,OR" << std::endl;
    int num1, num2;
    num2 && num1; // Logical AND    Checks if one or both values are true.
    num1 || num2; // Logical OR     Either of the values can be true

    if (num) {};
    if (num == true) {}; // The same as 15 but longer form. Use the perl style like line 15 ;)
    
    return 0;
}


